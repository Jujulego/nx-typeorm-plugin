{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "db-migrate executor",
  "description": "",
  "type": "object",
  "properties": {
    "database": {
      "type": "string",
      "description": "Database to migrate.",
      "default": "default"
    }
  },
  "required": ["database"]
}
